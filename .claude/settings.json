{
  "$schema": "https://code.claude.com/schemas/settings.json",
  "_comment": "Project-level Claude Code settings. Hooks below auto-trigger the porting-self-challenge critic at major step completion / session wrap-up. PreToolUse ship_claim_audit.py is a MECHANICAL honesty gate on outbound Discord messages (adapted from a5_ops; added 2026-06-02 after the owner noted I lacked a5_ops-style safety-net). See docs/_meta/SKILLS-USAGE.md §Hooks.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__plugin_discord_discord__reply|mcp__plugin_discord_discord__edit_message",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR\"/src/scripts/workflow/ship_claim_audit.py"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "Session is ending. Run /porting-self-challenge to perform the critic self-check defined in src/skills/_shared/porting-self-challenge/SKILL.md. Take as context: this session's git log on the working branch + any [SELF-CHALLENGE FAIL] tracebacks already posted to Discord. If the 10 questions reveal a new failure mode, append a porting_lesson and a challenge_pattern before exiting."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "if": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/critic-on-significant-commit.sh"
          }
        ]
      }
    ]
  }
}
