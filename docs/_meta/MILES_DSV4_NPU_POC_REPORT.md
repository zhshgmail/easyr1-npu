# [moved] miles + DSv4-Flash on NPU PoC 报告

> **该文件 2026-06-01 已迁移**到 [`output/miles-dsv4-flash-poc/docs/REPORT.md`](../../output/miles-dsv4-flash-poc/docs/REPORT.md)。
>
> 理由:借鉴 a5_ops `output/<slug>/` 模式,每个独立 sub-project 自带一个标准结构化 home(`PROJECT.json` + `README.md` + `docs/{REPORT,REPRODUCE,kb_index}.md` + `artifacts/`)。详见 [`output/README.md`](../../output/README.md)。
>
> 旧路径保留这一行 stub 是为了:
> 1. 已发出的 commit message / issue / PR / Discord 消息里的引用不立刻 broken
> 2. 任何 grep 落地到这里都能找到正确新地址
>
> 删除这个 stub 的时机:6 个月后 (~2026-12) 没有新引用时。届时直接 `git rm`,在 changelog 记一笔。
