# Handoff — state of work as of 2026-04-19

This doc captures where EasyR1→NPU port work stands at the handoff point, what's committed, what's in flight, and what the next session should pick up. Pairs with `DELIVERABLE.md` (sign-off view), `PORT-SUMMARY.md` (playbook view), and `transformers-upgrade-drill.md` (v2 drill outcome).

## 1. Committed and shipped

### v1 port (primary deliverable, 8.5.0 image)
- Branch `ascend-port` on EasyR1, 21 commits since `6f8197f`. All smoke-ladder levels V1.1 → V2.2 PASS.
- Two drill-originated code fixes cherry-picked onto mainline (backward-compat, safe for 8.5.0 users):
  - `1f716ea` — `no_init_weights` import compat (transformers <5 / ≥5 two-path).
  - `ecce71d` — `SamplingParams` read-only property skip in vllm 0.18.
- Pushed to `personal/ascend-port` on github.

### v2 drill (image upgrade rehearsal, 8.5.2 image)
- Branch `ascend-port-transformers-upgrade` on EasyR1, head `2fd9337`. PASSED end-to-end:
  - 2-step smoke: `entropy_loss=1.434` at step 1, matches V1.4 baseline.
  - 20-step trajectory smoke: entropy_loss stays in [1.31, 1.83], grad_norm max ~3.2, checkpoint saved.
- Full report: `repo/docs/transformers-upgrade-drill.md`.
- Image built on A3: `easyr1-npu-852:drill` (deef749e6444, 24.3GB).
- Four new NPU-* catalog entries added during the drill — BUG-004 + OPS-006/007/008.
- Pushed to `personal/ascend-port-transformers-upgrade` on github.

### Knowledge catalog (`repo/knowledge/npu-patterns.md`)
- Total: **23 stable IDs** (CP x7, BUG x4, ENV x4, OPS x8).
- Growth during drill: 19 → 23 (+4 all operational/infra).

### Skills (`repo/skills/`)
- 7 shipped: `npu-image-inspect`, `npu-code-path-sweep`, `npu-container-runner`, `upstream-branch-hygiene`, `codex-review`, `ray-npu-shim`, `image-upgrade-drill` (new, V0.3 of skills-design.md).
- Drill promoted to the `image-upgrade-drill` skill with a 7-step playbook (`repo/skills/image-upgrade-drill/SKILL.md`).

### Memories
- `a3_server.md`, `discord_cadence.md`, `a3_is_firewalled.md`, `codex_review_usage.md`, `version_aware_reviews.md`, `a3_docker_proxy.md`.

## 2. In flight / partial

### Drill reproduction by isolated agent (ongoing as of 2026-04-19 17:00)
- Isolated agent `ac31ee809fdc8dc57` was spawned to re-execute `image-upgrade-drill` from scratch as a skill-audit exercise.
- Got through Step 1-3 (infra pre-flight + drill branch + image build), kicked off V2.2 smoke, hit the expected first break (`no_init_weights` ImportError) at 16:36, **then stalled for 8 hours with no forward progress**.
- Built image `easyr1-npu-852:drill-reproduce` and branch `ascend-port-transformers-upgrade-reproduce` exist on A3; the log at `/tmp/z00637938/reproduce/logs/v22_reproduce_20260419_163542.log` is stuck at 2.2K.
- **Cause**: isolated agent harness limitation — either context saturation or timeout. The parent session has no SendMessage to poke it mid-run.
- **Conclusion**: the skill itself validated through Step 4's first break. Steps 4-7 would need to be executed either by a person or by breaking the drill into smaller agent-bounded tasks.

### Not yet started
- Cleanup of A3 reproduce artifacts (`/tmp/z00637938/reproduce/`, `easyr1-npu-852:drill-reproduce`, branch `ascend-port-transformers-upgrade-reproduce`). Left in place so the next session can either continue or delete.
- Skill refinement based on the isolated-agent experiment. Candidate note to add: "When to use … Don't spawn a single isolated agent for all 7 steps; the harness times out. Use an agent per step or keep it in the main session."

## 3. Open follow-ups (none blocking)

From `PORT-SUMMARY.md` Known-debt and the drill report:

1. **NPU-BUG-003 stays unresolved on CANN 8.5.1** — compile path now silently corrupts step-1 numerics before crashing at step-2; workaround (`use_torch_compile=false`) remains required. Reprobe when torch_npu 2.10 / triton-ascend 3.3+ ship.
2. **Full convergence run** (~500 steps) on the 8.5.2 image to prove stability past short trajectory. Not on critical path until the team decides to promote 8.5.2 to default.
3. **`Dockerfile.npu-852` → `Dockerfile.npu` promotion** if/when 8.5.2 is adopted as the default. Needs team approval.
4. **Drill methodology improvement**: the isolated-agent reproduction experiment demonstrated the skill is readable but long-running drills don't fit one agent invocation. Consider: (a) adding "split across agents" guidance to the skill, or (b) leaving as a "human + checklist" skill permanently.
5. **Ascend release watch**: new CANN / torch_npu / vllm-ascend / triton-ascend tags should trigger a fresh `image-upgrade-drill` run. No schedule needed — drive on demand.

## 4. How to resume

If the next session wants to finish the drill reproduction:
- Branch already exists: `ascend-port-transformers-upgrade-reproduce`.
- Image already built: `easyr1-npu-852:drill-reproduce` on A3.
- Resume at step 4 of `image-upgrade-drill` skill — fix `no_init_weights` import in `verl/workers/fsdp_workers.py:40`, then fix `SamplingParams.eos_token_id` setattr in `verl/workers/rollout/vllm_rollout_spmd.py:173`, scp both to A3, rerun V2.2 smoke, then run the 20-step probe.
- Expect to finish in ~30 min wall-clock.

If the next session wants to pick a brand new task, pointers:
- `repo/docs/DELIVERABLE.md` — official sign-off for v1.
- `repo/docs/PORT-SUMMARY.md` — playbook for porting EasyR1 to newer versions or porting adjacent RL frameworks.
- `repo/skills/image-upgrade-drill/SKILL.md` — for the next image/framework bump.

## 5. Contact / state

- Git remotes are authenticated from the **local workstation**, not A3 (A3 has no cached creds). Any new push flow: commit locally → `git push personal <branch>` → on A3 use `scp` to distribute file-level changes, or on A3 `git fetch personal` then `git reset --hard` (works if the branch already exists there from a prior push).
- A3 Ray-related processes under `chj_roll` conda env belong to **another user** — don't touch.
- Discord channel for async updates: `1494825170399924366`. Cadence: milestones + any >15-min silence gets a short heartbeat.
